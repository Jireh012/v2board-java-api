# Implement

1. Fix `getBonus` nested path; harden list/array parsing; tests.
2. Spec note in `admin-commerce.md`.
3. UI `order.ts` + ProfileView deposit modal.
4. Run tests via Docker Maven if needed.
