# Implement checklist

1. ConfigService: `getShowInfoToServerEnable`; fix `buildSubscribeUrl` to method 0.  
2. ClientController: ConfigService gate + method filter; unit-testable private/package helper or focused test.  
3. StatController: expose `show_subscribe_expire`.  
4. UI DashboardHome + SubscribeInfo type.  
5. yml comment fix; spec note in subscribe-delivery.md.  
6. Tests + lint.
