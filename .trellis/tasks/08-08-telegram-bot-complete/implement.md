# Implement

1. Expand TelegramService + GuestTelegramController + TelegramBotHandler
2. Ticket/admin notify hooks
3. Public discuss_link + Profile UI
4. Unit tests for token auth, bind parse, enable flag
5. Spec backend/telegram.md (new) + public-site-config note
