package com.v2board.api.config;

import org.springframework.context.annotation.Configuration;

/**
 * Legacy placeholder — subscribe route registration lives in {@link SubscribeRouteRegistrar}.
 * Kept so existing docs/imports referencing DynamicRouteConfig remain discoverable.
 */
@Configuration
public class DynamicRouteConfig {
}
